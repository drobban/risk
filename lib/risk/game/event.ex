import EnumType

defenum Risk.Game.Event do
  value Done, :done
  value GetStatus, :get_status
  value Deploy, :deploy
  value ColorSelect, :color_select
  value Ready, :ready
  value Connection, :connection
end
