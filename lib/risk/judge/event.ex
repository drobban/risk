import EnumType

defenum Risk.Judge.Event do
  value(AddPlayer, :add_player)
  value(Done, :done)
  value(GetPlayOrder, :get_play_order)
  value(GetStatus, :get_status)
  value(NextPlayer, :next_player)
  value(NextPhase, :next_phase)
end
